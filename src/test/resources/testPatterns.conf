ExtractionPatterns = {
  patterns = [
    {
      name = "result-percent",
      pattern = "CD {%|percent|per cent|pct}"
    },
    {
      name = "treatments",
      pattern = "{were given|treated with|received|receiving} {CD|NN|JJ|IN}+"
    }
  ]
}
